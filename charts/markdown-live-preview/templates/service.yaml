apiVersion: v1
kind: Service
metadata:
  name: {{ include "markdown-live-preview.fullname" . }}
  labels:
    {{- include "markdown-live-preview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "markdown-live-preview.selectorLabels" . | nindent 4 }}
